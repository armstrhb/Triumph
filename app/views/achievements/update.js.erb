update_field("realm-title", "<%= @achievement.title %>");
update_field("realm-description", "<%= @achievement.description %>");
update_field("realm-category", "<%= @achievement.category.name %>");
update_field("realm-rarity", "<%= @achievement.rarity.name %>");

if (<%= @achievement.active? %>)
  $("#active-banner").fadeOut(250);
else
  $("#active-banner").fadeIn(250);

$('#edit-modal').modal_success();
